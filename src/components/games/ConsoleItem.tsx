import { FC } from "react";
import IGame from "../../interfaces/IGame";

const GameItem: FC<IGame> = ({id, title, description, platform, releaseYear, publisher, genre, price, heroImage, inGameImage1, inGameImage2,inGameImage3,inGameImage4}) => {
    return(
        <article></article>
    )
}

export default GameItem