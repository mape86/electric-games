import { FC } from "react";
import IConsole from "../../interfaces/IConsole";

const ConsoleItem: FC<IConsole> = ({id, model, brand, price, description, image}) => {
    
    return (
        <article></article>
    )
}

export default ConsoleItem